{
  "name": "Comprehensive Test Plugin",
  "version": "1.0.0",
  "description": "A comprehensive test plugin that implements all available hooks to verify the plugin architecture works correctly",
  "author": "Services AI Team",
  "email": "plugins@servicesai.com",
  "entry_point": "main.py",
  "plugin_class": "ComprehensiveTestPlugin",
  "permissions": [
    "read_leads",
    "write_leads",
    "read_bookings",
    "write_bookings",
    "read_invoices",
    "write_invoices",
    "send_notifications",
    "access_api",
    "dashboard_widget",
    "modify_ui"
  ],
  "settings": [
    {
      "name": "enable_logging",
      "type": "checkbox",
      "default": "true",
      "description": "Enable detailed logging for all hook executions"
    },
    {
      "name": "notification_level",
      "type": "select",
      "options": ["all", "important", "critical"],
      "default": "all",
      "description": "Level of notifications to send"
    },
    {
      "name": "test_mode",
      "type": "checkbox",
      "default": "true",
      "description": "Run in test mode (logs only, no actual modifications)"
    }
  ]
}
